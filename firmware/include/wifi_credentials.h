#ifndef WIFI_CREDENTIALS_H
#define WIFI_CREDENTIALS_H

/*
 * WiFi Telemetry Configuration
 *
 * WiFi credentials are configured via Kconfig using
 * CONFIG_WIFI_CREDENTIALS_STATIC_SSID and CONFIG_WIFI_CREDENTIALS_STATIC_PASSWORD.
 */

#define TELEMETRY_IP    "192.0.2.1"
#define TELEMETRY_PORT  8080

#endif
